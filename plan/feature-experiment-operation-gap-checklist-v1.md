# 实验操作库与实验操作待开发评审清单

> 用途：逐项确认《实验操作库与实验操作》需求在 Uni-Lab OS 本地模式中的实现范围。
> 需求来源：[飞书需求文档](https://dptechnology.feishu.cn/wiki/MgLpw1xh0iqGx8kPl60cMv99nfc)
> 核对仓库：`Uni-Lab-OS-github`
> 核对分支：`product/durable-scheduler-kernel-v2`
> 核对基线：`0242959f`
> 核对日期：2026-08-31

## 1. 核对范围与已确认口径

- “实验操作”按可发布、可被其他工作流引用的工作流（Workflow）实现，不再建立另一套定义模型。
- 忽略需求文档中已经删除的内容。
- Agent 和权限不在本轮范围内。
- “设备操作、集成操作、人工操作”等实验操作类别由领域包持有稳定身份，支持新增、改名、调整顺序和删除；工作流标签继续作为普通标签，不再兼任类别权威。
- 当前接口状态值 `source`、`published` 可分别显示为“草稿”“已验证”，本轮不另造一套状态值。
- 已发布实验操作可被其他工作流复用；引用方保存静态展开结果，运行时不再动态创建第二个工作流任务（WorkflowTask）。

## 2. OS 待开发总表

| 序号 | 状态 | 优先级 | 待开发功能 | 对应文档需求 | 负责仓库 |
|---|---|---|---|---|---|
| OS-01 | [x] 已实现 | P0 | 实验操作类型、类别管理与列表筛选 | 实验操作库：只查询实验操作，按状态和类别筛选，并维护类别 | Uni-Lab-OS |
| OS-02 | [ ] 待确认 | P0 | 补齐循环、条件、等待控制节点 | 实验操作定义与编排：支持循环、条件、等待、人工确认、并行 | Uni-Lab-OS |
| OS-03 | [ ] 待确认 | P0 | 参数单位 | 参数与数据契约：参数包含类型、单位、默认值、是否必填、允许范围 | Uni-Lab-OS |
| OS-04 | [ ] 待确认 | P1 | 局部变量和全局变量 | 参数与数据契约：参数值可来自固定值、局部变量或全局变量 | Uni-Lab-OS |
| OS-05 | [ ] 待确认 | P1 | 节点运行策略 | 实验操作定义与编排：超时、重试、失败处理和日志策略 | Uni-Lab-OS |
| OS-06 | [ ] 待确认 | P1 | 发布前差异与影响分析 | MANUAL-01、MANUAL-02：发布前查看差异、影响范围和调试结果 | Uni-Lab-OS |
| OS-07 | [ ] 待确认 | P1 | 查询引用当前实验操作的工作流 | 工作流中的复用：查看哪些工作流引用当前实验操作 | Uni-Lab-OS |
| OS-08 | [ ] 待确认 | P1 | 统一调试错误定位信息 | 调试与排障：定位到实验操作、内部节点和参数 | Uni-Lab-OS |

## 3. OS 待开发项逐项确认

### OS-01 实验操作类型与列表筛选

| 项目 | 内容 |
|---|---|
| 文档需求 | 实验操作库支持按名称搜索，按类型、状态筛选；默认按设备操作、集成操作、人工操作分类展示；列表展示名称、类型和草稿/已验证状态。 |
| 当前实现 | 工作流定义包含 `workflow_type=normal\|experiment_operation`，省略时默认 `normal`；类型在创建时决定 `workflows/` 或 `experiment_operations/` 目录，后续更新不能跨目录改类型。类别接口提供设备操作、集成操作、人工操作三个默认类别，支持查、新增、改名、调整顺序和删除；类别写入唯一领域包的 `operation_categories.json`，不新增 SQLite 表。发布合同写入同一领域包的 `workflow_publications.json`，OS 重启后恢复原合同 UUID、版本和 `published` 状态；运行事实 SQLite 不保存工作流定义或发布合同。实验操作通过稳定的 `operation_category_uuid` 引用类别，类别改名不影响工作流。`GET /api/v1/workflows` 可按 `workflow_type`、`status`、名称和类别组合查询，省略新增条件仍返回全部。旧 `device_operation`、`integration_operation` / `integrated_operation`、`manual_operation` 标签仍能映射到默认类别。 |
| 前端接入 | 实验操作库先读取 `GET /api/v1/experiment-operation-categories`，再以 `workflow_type=experiment_operation` 一次读取实验操作，并按返回的 `operation_category_uuid` 分组；只看单一类别时可追加同名查询参数。类别管理使用同一路径的 POST、PUT、DELETE。 |
| 删除规则 | 类别仍被任一实验操作引用时拒绝删除；先修改或清空这些实验操作的 `operation_category_uuid`，再删除类别。 |
| 验收条件 | 类别 CRUD、跨 OS 重启恢复、实验操作绑定与筛选、普通工作流拒绝挂类别、引用删除保护和旧标签兼容均通过公共 HTTP 回归。 |

确认：

- [x] 按上述范围开发
- [ ] 调整范围
- [ ] 本期不做

结论记录：

> 已按兼容方案实现：创建不传类型仍是普通工作流，列表不传类型、状态和类别仍
> 查询全部；旧版更新不传类型和类别会保留原值。类别具有稳定 UUID，名称可改，
> 不会因为改名破坏工作流引用；旧类别标签继续兼容，但新写入以类别 UUID 为准。

### OS-02 补齐循环、条件、等待控制节点

| 项目 | 内容 |
|---|---|
| 文档需求 | 实验操作可以包含设备动作、其他实验操作和控制节点；控制节点包括循环、条件、等待、人工确认和并行。 |
| 当前实现 | 并行和人工确认已有主要闭环；循环、条件、等待还没有形成从创作、校验、编译到调度执行的完整闭环。条件节点可被部分图校验识别，但当前工作流编译器不能完整执行。 |
| 待开发 | 分别定义循环、条件、等待节点的数据格式；补保存校验、编译规则、运行状态、失败语义和调度测试。 |
| 安全边界 | 循环不能破坏工作流有向无环图（DAG）的静态结构；可采用受控展开或控制节点语义，不能直接形成无法判定的图环。 |
| 验收条件 | 三类控制节点均能创建、保存、重新加载、编译和运行；非法条件、无限循环风险和缺失等待参数会在执行前被拒绝。 |

确认：

- [ ] 循环、条件、等待全部开发
- [ ] 只开发其中部分，范围见结论记录
- [ ] 本期不做

结论记录：

> 待填写。

### OS-03 参数单位

| 项目 | 内容 |
|---|---|
| 文档需求 | 参数定义包含数据类型、单位、默认值、是否必填和允许范围。 |
| 当前实现 | 已支持类型、默认值、是否必填和 JSON Schema 范围约束；正式参数描述中没有独立的单位字段。 |
| 待开发 | 在工作流参数描述和已发布合同的 `parameters` 中增加可选单位信息；导入、保存、发布、读取和实验操作展开时不得丢失。 |
| 兼容策略 | 单位为可选字段；未填写单位的旧领域包工作流继续正常加载。 |
| 验收条件 | 参数单位能够完整往返；发布后前端能读到；实验操作嵌入工作流后仍保留。 |

确认：

- [ ] 按上述范围开发
- [ ] 单位仅作展示，不参与换算
- [ ] 单位还需要参与自动换算，另行定义规则
- [ ] 本期不做

结论记录：

> 待填写。

### OS-04 局部变量和全局变量

| 项目 | 内容 |
|---|---|
| 文档需求 | 参数值可以是固定值、上游局部变量或全局变量；输出参数可以暴露为局部变量或全局变量。 |
| 当前实现 | 固定值、父工作流参数和上游节点输出绑定已经支持；当前正式创作模型没有独立、明确的局部变量和全局变量定义。 |
| 待开发 | 明确变量的标识、数据类型、来源、作用范围和生命周期；补参数绑定格式、保存校验、运行时解析和错误返回。 |
| 需要先决定 | “全局”是仅在一次工作流任务（WorkflowTask）内共享，还是跨工作流任务持久保存。两者语义不同，不能共用一个模糊字段。 |
| 验收条件 | 前端能明确知道每个参数来自固定值、父参数、上游输出或全局变量；不存在同名变量静默覆盖或类型不匹配。 |

确认：

- [ ] 只保留现有固定值、父参数和上游输出，不新增变量模型
- [ ] 增加任务内共享变量
- [ ] 增加跨任务持久全局变量，需要单独设计权威和存储
- [ ] 本期不做

结论记录：

> 待填写。

### OS-05 节点运行策略

| 项目 | 内容 |
|---|---|
| 文档需求 | 实验操作可以配置超时、重试、失败处理和日志策略。 |
| 当前实现 | 超时已有部分支持，系统具备运行日志；节点级重试、失败处理和日志策略没有统一、稳定的公开格式。 |
| 待开发 | 定义统一的节点运行策略，并明确哪些策略由调度器（Scheduler）执行、哪些只控制日志展示。 |
| 安全边界 | 投递重放（DeliveryReplay）不等于执行重试（ExecutionRetry）。当前没有充分物理证据时，真实设备动作不得自动执行重试，避免重复加液、移动或加热。 |
| 验收条件 | 策略能保存、发布和执行；非法策略在运行前拒绝；设备动作重试遵守执行安全约束。 |

确认：

- [ ] 只实现超时、失败处理和日志策略，设备执行重试保持禁止
- [ ] 设计完整的安全执行重试方案后再实现重试
- [ ] 本期不做

结论记录：

> 待填写。

### OS-06 发布前差异与影响分析

| 项目 | 内容 |
|---|---|
| 文档需求 | 发布前查看结构、参数、输入输出和调试结果；修改已验证实验操作后查看前后差异及影响范围。 |
| 当前实现 | OS 内部能够判断实验操作更新是否兼容，并能对兼容引用方进行刷新；没有面向前端的完整发布前差异报告接口。 |
| 待开发 | 增加只读检查接口，对比当前已应用工作流图和最新发布合同，返回节点变化、参数变化、输入输出变化、受影响父工作流和阻断原因。 |
| 验收条件 | 检查接口不修改工作流；相同内容结果为空；参数删除、改名、类型变化和必填参数新增能明确报告。 |

确认：

- [ ] 按上述范围开发
- [ ] 只返回接口合同差异，不返回内部节点差异
- [ ] 由前端自行比较，OS 只提供原始数据
- [ ] 本期不做

结论记录：

> 待填写。

### OS-07 查询引用当前实验操作的工作流

| 项目 | 内容 |
|---|---|
| 文档需求 | 用户可以查看哪些工作流引用了当前实验操作；修改时能够看到影响范围。 |
| 当前实现 | 已提供 `GET /api/v1/workflows/{workflow_uuid}/referenced-by`，按当前已应用图返回直接或静态展开后仍包含该实验操作的工作流摘要，并支持分页；查询不新增持久索引。 |
| 后续可选 | 若前端需要精确定位画布，可在现有摘要上补调用节点 UUID 和兼容性诊断；当前“被哪些工作流引用”已经闭环。 |
| 验收条件 | 无引用返回空列表；直接引用和多层静态展开引用均能查到；目标不存在返回稳定错误；查询不修改任何工作流。 |

确认：

- [x] 返回直接和间接引用
- [ ] 只返回直接引用
- [ ] 本期不做

结论记录：

> 已按“返回直接和多层静态展开引用”实现；节点级定位和发布前兼容性差异留在 OS-06。

### OS-08 统一调试错误定位信息

| 项目 | 内容 |
|---|---|
| 文档需求 | 调试失败后定位到具体实验操作、内部节点或参数，并显示节点状态、结果、日志和 Trace。 |
| 当前实现 | 工作流任务（WorkflowTask）、工作流节点作业尝试（WorkflowNodeJobAttempt）、事件、反馈、日志和 Trace 的基础数据已经存在，但不同错误路径返回的节点和参数定位格式不完全统一。 |
| 待开发 | 统一调试错误结构，至少返回工作流 UUID、节点 UUID、参数名、错误类型、可读原因和 Trace 标识。 |
| 验收条件 | 前端无需解析错误文本即可定位节点和参数；不确定执行使用 `execution_unknown`，不能被普通失败覆盖。 |

确认：

- [ ] 按上述范围开发
- [ ] 先统一节点定位，参数定位后续补充
- [ ] 本期不做

结论记录：

> 待填写。

## 4. 前端待核对或开发项

> 本节来自需求文档中的交互要求。是否已经实现，需要在 `uni-lab-fe` 仓库单独核对；不能仅凭 OS 代码判断完成。

| 序号 | 状态 | 前端事项 | 对应文档需求 | 依赖的 OS 能力 |
|---|---|---|---|---|
| FE-01 | [ ] 待核对 | 按设备操作、集成操作、人工操作分类展示；按名称、类型、状态筛选 | 实验操作库 | OS-01 |
| FE-02 | [ ] 待核对 | 工作流树、画布、源码和参数面板同步选中同一节点 | 实验操作定义与编排；参数与数据契约 | 现有节点 UUID 与工作流图接口 |
| FE-03 | [ ] 待核对 | 参数填写支持固定值、父工作流参数、上游输出及后续变量来源 | 参数与数据契约 | OS-03、OS-04 |
| FE-04 | [ ] 待核对 | 输入输出中同时配置参数、物料及其内部映射 | MANUAL-01 第 9～11 步 | 现有输入输出合同和物料占位符（ResourceSlot） |
| FE-05 | [ ] 待核对 | 单步、断点、完整运行调试；展示节点状态、结果、日志和 Trace | 调试与排障 | 现有调试接口、OS-08 |
| FE-06 | [ ] 待核对 | 从错误跳到节点或源码，返回后恢复选择、缩放和滚动位置 | 调试与排障；工作流中的复用 | OS-08 |
| FE-07 | [ ] 待核对 | 发布前展示差异、影响范围和调试结果 | MANUAL-01、MANUAL-02 | OS-06、OS-07 |

## 5. 已实现，不重复开发

| 序号 | 已有能力 | 当前结论 |
|---|---|---|
| DONE-01 | 创建、编辑、保存和读取工作流（Workflow） | 已实现 |
| DONE-02 | 发布实验操作，并以 `published` 表示可供其他工作流复用 | 已实现 |
| DONE-03 | 将已发布工作流嵌入父工作流并静态展开 | 已实现 |
| DONE-04 | 检查直接和间接循环引用 | 已实现 |
| DONE-05 | 实验操作参数来自外层工作流参数、上游输出、节点固定值或默认值 | 已实现 |
| DONE-06 | 实验操作兼容更新时自动刷新引用方；不兼容时保留引用方原有效定义并报告问题 | 已实现 |
| DONE-07 | 已创建的工作流任务使用冻结执行计划，不受后续工作流修改影响 | 已实现 |
| DONE-08 | 并行节点和人工确认 | 已实现 |
| DONE-09 | 单步、断点和完整流程调试的主要接口 | 已实现 |

## 6. 文档口径需要修正，但暂不进入开发队列

| 序号 | 文档冲突 | 当前确认口径 | 处理方式 |
|---|---|---|---|
| DOC-01 | 文档出现“修改组名和新增组” | 已确认为实验操作类别管理，类别持久化到领域包 | OS-01 已实现并同步接口说明 |
| DOC-02 | 文档写引用方不自动跟随实验操作更新 | 当前产品决定为：兼容更新自动替换，不兼容时保留旧定义并提示 | 修改需求文档文字 |
| DOC-03 | 文档保留“每次保存生成不可变版本”，但多处版本历史需求已删除 | 当前只明确区分源码和已发布状态；是否保留每次保存的版本历史尚未确认 | 产品确认前不开发 |

## 7. 逐项评审记录

| 评审顺序 | 项目 | 最终结论 | 负责人 | 备注 |
|---|---|---|---|---|
| 1 | OS-01 实验操作类型、类别与列表筛选 | 已实现 | Uni-Lab-OS | `workflow_type`、`status`、`operation_category_uuid` 组合筛选；类别独立 CRUD，旧标签兼容 |
| 2 | OS-02 控制节点 | 待确认 |  |  |
| 3 | OS-03 参数单位 | 待确认 |  |  |
| 4 | OS-04 局部变量和全局变量 | 待确认 |  |  |
| 5 | OS-05 节点运行策略 | 待确认 |  |  |
| 6 | OS-06 发布前差异与影响分析 | 待确认 |  |  |
| 7 | OS-07 工作流引用关系查询 | 已实现 | Uni-Lab-OS | `GET /api/v1/workflows/{workflow_uuid}/referenced-by` |
| 8 | OS-08 调试错误定位信息 | 待确认 |  |  |
| 9 | FE-01～FE-07 前端事项 | 待核对 |  |  |

## 8. 文件规模与拆分决定

本轮修改触及以下既有大文件，但没有继续向其中堆叠可独立职责：

| 文件 | 当前规模 | 本轮职责与处理决定 |
|---|---:|---|
| `unilabos/app/workflow_api.py` | 1,576 行 | 保留稳定工作流路由和查询兼容；类别 CRUD 已拆到 `operation_category_api.py`。后续若继续增加独立资源，应继续拆路由模块。 |
| `unilabos/workflow/authoring_ast.py` | 1,523 行 | 只让 AST 根声明读取 `workflow_type`，不新增编译阶段；单独拆分会同时改动现有语法校验边界，本轮不做。 |
| `unilabos/workflow/authoring_graph.py` | 1,444 行 | 只把工作流类型带入图投影，不新增职责，本轮保留。 |
| `unilabos/workflow/authoring_python.py` | 1,396 行 | 只让规范 Python 往返保留工作流类型，不新增生成器，本轮保留。 |
| `unilabos/workflow/service.py` | 6,409 行 | 只保留类别与工作流之间的引用完整性、领域源码创建、发布目录协调和引用查询编排；类别文件读写已拆到 `operation_category.py`，发布文件读写已拆到 `publication_catalog.py`，类别 HTTP 已拆到独立路由。后续服务拆分应以创作、发布、运行三个深模块为边界另立重构任务。 |
| `unilabos/workflow/store.py` | 3,894 行 | 只保留 `workflow_type` 存取和筛选；工作流定义由领域包文件重建到内存目录，不增加旧类型值的 SQLite 迁移，类别文件逻辑不进入仓储。 |
| `unilabos/workflow/composite_expansion.py` | 1,453 行 | 仅把已有组合调用的领域表述统一为“实验操作”，不改变展开算法或增加分支；本轮不拆。 |
| `unilabos/workflow/source_workspace.py` | 715 行 | 将同一安全文件访问边界扩展到 `experiment_operations/*.py`，普通工作流与实验操作共享验证实现；低于 800 行硬门槛，本轮保留。 |
| `unilabos/workflow/composition.py` | 779 行 | 只在 Python 来源固定点激活后增加发布合同恢复步骤，保持“来源→发布合同→运行任务恢复”的启动顺序；低于 800 行硬门槛，本轮不拆组合根。 |
| `unilabos/workflow/composite_compatibility.py` | 692 行 | 仅统一组合调用中的实验操作术语，没有修改合同兼容判定；低于 800 行硬门槛，本轮保留。 |
| `tests/app/test_workflow_publication_api.py` | 547 行 | 在同一发布与组合调用公共 HTTP 合同中补反向引用回归；低于 800 行硬门槛，本轮保留。 |
| `unilabos/workflow/composite_contract_refresh.py` | 530 行 | 仅统一组合刷新中的实验操作术语，没有增加刷新职责；低于 800 行硬门槛，本轮保留。 |
| `tests/workflow/test_source_definition_bootstrap.py` | 578 行 | 只补冷启动期望字段，未增加新测试场景；低于 800 行硬门槛，暂按同一启动合同保留。 |
| `unilabos/workflow/published_contract.py` | 1,008 行 | 发布合同的生成、进程内恢复和公共投影必须共享同一摘要与模板投影不变量；本轮只增加文件快照恢复和失败补偿，不拆出第二套合同模型。后续可把 SQLite 投影 SQL 下沉为仓储模块。 |
| `tests/workflow/test_domain_workflow_source_sync.py` | 664 行 | 同一公共服务接缝验证 Python 权威、分目录创建、发布文件、删除故障收敛和冷启动恢复；低于 800 行硬门槛，本轮保留。 |

新增 `operation_category.py` 和类别 HTTP/回归测试均低于 500 行；类别目录保持一个深模块，调用方不接触文件 CAS、默认值投影或 JSON 校验细节。

## 9. 验证记录

- 工作流目录与专项接口回归：`918 passed, 5 skipped`；覆盖类型、类别、状态、名称模糊搜索、描述、领域包分目录、Python 导入、发布合同重复/新版发布、冷启动恢复、删除故障收敛和引用查询。
- 其余不依赖 ROS2 的应用层回归：`522 passed, 1 xfailed`；完整应用层收集受本机缺少 `action_msgs` / `rclpy` 限制，相关失败不在本次修改链路。
- 本文是需求核对与开发决策清单，不代表尚未开发的功能已经完成。
- 每确认一项，应同步更新本文件的状态、结论、接口范围和验收条件，再进入开发。
